<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jkpark.bucketlist_unit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.TestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.SplashActivity" />
        <activity android:name=".activity.MakeBucketActivity" />
        <activity android:name=".activity.PopupActivity" />
        <activity android:name=".activity.RecommendActivity" />
        <activity android:name=".activity.BucketDetailActivity" />
        <activity android:name=".activity.RankingActivity" />
        <activity android:name=".activity.BucketProgressActivity"></activity>
    </application>

</manifest>